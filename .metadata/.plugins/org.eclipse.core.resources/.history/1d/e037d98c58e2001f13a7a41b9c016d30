package com.aurionpro.Array;

import java.util.*;

public class SortedSquareOfElements {
	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);

		System.out.print("Enter the size of the array: ");
		int size = scanner.nextInt();
		
		System.out.println();
		
		int[] array = new int[size];
		
		for (int i = 0; i < array.length; i++) {
			System.out.print("Enter number "+(i+1)+": ");
			array[i] = scanner.nextInt();
		}
		
		System.out.println();
		sort(array);
		
		System.out.println("Input Array is: ");
		for (int i : array) {
			System.out.print(i+" ");
		}
		
		System.out.println();
		
		
		
		scanner.close();
	}
	
	public static int[] sortedArray(int array[])
	{
		int[] sortedArray = new int[array.length];
		int left = 0, right = array.length - 1, index = array.length - 1;
		
		while(left <= right)
		{
//			int leftSquareValue = array[left] * array[left];
//			int rightSquareValue = array[right] * array[right];
			
			if(array[left] > array[right])
			{
				sortedArray[index] = array[left];
				left++;
			}
			else
			{
				sortedArray[index] = array[right];
				right--;
			}
			index--;
		}
		return sortedArray;
	}
	
	public static int[] square(int[] array)
	{
		for (int i = 0; i < array.length; i++) {
			array[i] = array[i] * array[i];
		}
		
		return array;
	}
}
